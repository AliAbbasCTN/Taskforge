# Application
NODE_ENV=development
PORT=3000

# CORS - the frontend's origin, allowed to call this API with credentials.
# Update this once the Phase 09 frontend is running (default Vite port is 5173).
CORS_ORIGIN=http://localhost:5173
